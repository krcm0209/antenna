name: antenna
description: A radio station API powered by FCC broadcast data. Find every FM and AM station you can pick up at any location.
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-python
