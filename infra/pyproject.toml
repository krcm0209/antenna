[project]
name = "antenna-infra"
version = "0.0.0"
description = "Pulumi infrastructure for antenna"
requires-python = ">=3.14"
dependencies = [
    "pulumi>=3.0.0,<4.0.0",
    "pulumi-gcp>=9.0.0,<10.0.0",
]
